# ============================================================
#  Хороший полицейский — переменные окружения
#  Скопируйте этот файл в .env и заполните значения.
#  .env НЕЛЬЗЯ коммитить в git — там живые токены.
# ============================================================

# --- Telegram ---
# Токен от @BotFather (/newbot)
TELEGRAM_BOT_TOKEN=
# Чат курсантов/кураторов, куда приходят посты на модерацию.
# Узнать id: добавьте бота в чат и отправьте /chatid
MODERATOR_CHAT_ID=
# Канал, куда публикуются одобренные посты (бот должен быть админом)
PUBLISH_CHAT_ID=
# Telegram id администраторов через запятую (узнать: @userinfobot)
BOT_ADMIN_IDS=

# --- AI Provider (OpenAI-совместимый) ---
AI_PROVIDER_API_KEY=
AI_PROVIDER_BASE_URL=https://aiprovider.duckdns.org/v1
AI_PROVIDER_MODEL=claude-sonnet-4-6
# Необязательно: более дешёвая модель для массовой верификации
AI_PROVIDER_VERIFY_MODEL=

# --- Генерация изображений ---
# ВНИМАНИЕ: у aiprovider.duckdns.org НЕТ эндпоинта /images/generations
# (проверено — отвечает 404), поэтому картинки рисуются локально
# фирменной карточкой. Настройки ниже включат удалённую генерацию
# автоматически, если появится провайдер с таким эндпоинтом.
IMAGE_GENERATION_ENABLED=true
IMAGE_CARD_FALLBACK=true
AI_PROVIDER_IMAGE_MODELS=gpt-image-1,dall-e-3
AI_PROVIDER_IMAGE_SIZE=1024x1024

# --- Веб-панель модерации (обязательно, иначе панель не откроется) ---
PANEL_USERNAME=
PANEL_PASSWORD=

# --- Подпись под постами ---
CHANNEL_USERNAME=kindpolice
BOT_USERNAME=kindmvdbot
CHANNEL_TITLE=Хороший полицейский

# --- Сбор ---
FETCH_LIMIT=10
FETCH_PER_SOURCE=20
MAX_NEWS_AGE_DAYS=7
POST_INTERVAL_MINUTES=120

# --- Логирование ---
LOG_LEVEL=INFO
LOG_TO_FILE=true

# --- MAX (когда появится боевой токен) ---
MAX_BOT_TOKEN=
MAX_PUBLISH_CHAT_ID=

# --- ИИ-помощник для пользователей ---
# Отвечает на бытовые правовые вопросы. Ограничения (запрет называть
# номера статей, дисклеймер, стоп-темы) зашиты в код и настройкой
# не отключаются — см. pipeline/assistant.py.
ASSISTANT_ENABLED=true
# Модель помощника; пусто — берётся AI_PROVIDER_MODEL
ASSISTANT_MODEL=
# Вопросов на человека в сутки (0 — без лимита, в проде не рекомендуется)
ASSISTANT_DAILY_LIMIT=15
